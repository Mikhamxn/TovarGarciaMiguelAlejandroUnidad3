services:
  mimascota:
    build: .
    restart: always
    expose:
      - "3000"
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.mimascota.rule=Host(`gridstudio.app`)"
      - "traefik.http.services.mimascota.loadbalancer.server.port=3000"
